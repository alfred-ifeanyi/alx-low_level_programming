#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first integer to multiply with b
 * @b: second integer to multiply with a
 * Return: the result
 */

int mul(int a, int b)

{
	int i;

	i = a * b;

	return (i);
}
