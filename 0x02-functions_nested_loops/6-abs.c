#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: The integer should be computed
 * Return: Absolute value of the number
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
