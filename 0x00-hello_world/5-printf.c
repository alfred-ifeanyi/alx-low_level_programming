#include <stdio.h>

/**
 * main - print exactly with proper grammar, but the outcome is a piece of art,
 * Return: 0 if edited properly, non 0 otherwise
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
